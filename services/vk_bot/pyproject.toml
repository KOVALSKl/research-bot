[project]
name = "vk-bot"
version = "0.1.0"
description = "VK bot service (planned for v3)"
requires-python = ">=3.12"
dependencies = []

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["src/vk_bot"]
